name: "product_launch_analytics"
version: "1.0.0"
config-version: 2

# Profile lives alongside this file (analytics/dbt/profiles.yml); invoke dbt
# with --profiles-dir pointed here. See analytics/README.md.
profile: "analytics"

model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  product_launch_analytics:
    staging:
      +materialized: view
    marts:
      +materialized: table
